export const nextBigger = (n: number) => {
  // 同じ数字を使って、1つ数が大きくなるパターン

  /**
   * 全パターン出すは計算量が多い
   * 
   * 1桁目と2桁目比較      1桁目の方が大きければ入れ替え
   * 2桁目と3桁目を比較    2桁目の方が大きければ入れ替え
   *  2桁目と1桁目を比較   2桁目の方が小さければ入れ替え
   */
  
  return -1;
}